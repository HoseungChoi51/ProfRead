import{describe,expect,it}from'vitest';import{renderHtml,renderMarkdown}from'./render.js';
const base:any={title:'Study',sourceName:'study.html',html:'<html><head></head><body><p data-block-id="b">Quoted text</p></body></html>',artifacts:[],messages:[]};
describe('export placement',()=>{it('uses popovers, footnotes, and appendices at configured lengths',()=>{const html=renderHtml({...base,highlights:[{block_id:'b',exact_quote:'Quoted text',note:'short'},{block_id:'x',exact_quote:'medium',note:'m'.repeat(401)},{block_id:'y',exact_quote:'long',note:'l'.repeat(1201)}]});expect(html).toContain('data-note="short"');expect(html).toContain('<sup>1</sup>');expect(html).toContain('<h2>Appendix</h2>')});it('includes source footnotes in Markdown',()=>{const md=renderMarkdown({...base,highlights:[{exact_quote:'Quote',note:'Note'}]});expect(md).toContain('> Quote[^1]');expect(md).toContain('[^1]: Note')});});
