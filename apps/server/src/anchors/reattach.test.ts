import {describe,expect,it} from 'vitest';import {reattach} from './reattach.js';
describe('anchor reattachment',()=>{it('prefers exact quote with surrounding context',()=>{const found=reattach({exact:'same quote',prefix:'right ',suffix:' ending',startOffset:50},[{blockId:'a',text:'wrong same quote place',start:0,end:22},{blockId:'b',text:'right same quote ending',start:40,end:63}]);expect(found.blockId).toBe('b');expect(found.status).toBe('attached')});it('marks weak fuzzy candidates unmatched',()=>expect(reattach({exact:'cryptographic protocol',prefix:'',suffix:'',startOffset:0},[{blockId:'x',text:'banana orchard',start:0,end:14}]).status).toBe('unmatched'))});
