import {nanoid}from'nanoid';import {db,now,row}from'../db/index.js';import{ChatCompletionsProvider,ResponsesProvider}from'./providers.js';import{models,profiles,providerSetting}from'./settings.js';
const tokens=(s:string)=>Math.ceil(s.length/4);
export function ensureContexts(documentVersionId:string):void{if(row('SELECT document_version_id FROM context_cache WHERE document_version_id=? LIMIT 1',documentVersionId)||row('SELECT id FROM background_jobs WHERE document_version_id=? AND kind=\'contexts\' AND status IN (\'queued\',\'running\')',documentVersionId))return;const id=nanoid(),time=now();db.prepare(`INSERT INTO background_jobs(id,kind,document_version_id,status,progress,created_at,updated_at)VALUES(?,'contexts',?,'queued',0,?,?)`).run(id,documentVersionId,time,time);setImmediate(()=>void generate(id,documentVersionId));}
async function generate(jobId:string,versionId:string){const version=row<{canonical_text:string;token_estimate:number}>('SELECT canonical_text,token_estimate FROM document_versions WHERE id=?',versionId);if(!version)return;db.prepare(`UPDATE background_jobs SET status='running',progress=.1,updated_at=? WHERE id=?`).run(now(),jobId);try{if(version.token_estimate<1200){store(versionId,'brief',version.canonical_text);store(versionId,'study',version.canonical_text);}else{const all=models(),deepIds=profiles().find(p=>p.name==='deep')?.modelIds??[],model=all.find(m=>deepIds.includes(m.id)),setting=model?providerSetting(model.providerId):undefined,key=setting?process.env[setting.secret_env_name]:undefined;if(!model||!setting||!key)throw new Error('Deep profile provider is not configured');const briefMax=Math.min(3000,Math.floor(version.token_estimate*.25)),studyMax=Math.min(12000,Math.floor(version.token_estimate*.6));const prompt=`Create two faithful contexts from this article. Return JSON only: {"brief":"...","study":"..."}. Brief <= ${briefMax} tokens. Study <= ${studyMax} tokens. Preserve definitions, claims, qualifications, and section structure.\n\n${version.canonical_text}`,config=JSON.parse(setting.config_json||'{}');const provider=model.protocol==='openai-responses'?new ResponsesProvider(key,[model],setting.base_url):new ChatCompletionsProvider(key,[model],setting.base_url,model.protocol==='openrouter',config.provider);let output='';for await(const event of provider.run({model,messages:[{role:'user',content:prompt}]}))if(event.type==='text_delta')output+=event.delta;const parsed=JSON.parse(output.replace(/^```json\s*|\s*```$/g,''));if(typeof parsed.brief!=='string'||typeof parsed.study!=='string'||tokens(parsed.brief)>briefMax||tokens(parsed.study)>studyMax)throw new Error('Context output failed validation');store(versionId,'brief',parsed.brief);store(versionId,'study',parsed.study);}db.prepare(`UPDATE background_jobs SET status='completed',progress=1,updated_at=? WHERE id=?`).run(now(),jobId)}catch(error){db.prepare(`UPDATE background_jobs SET status='failed',error=?,updated_at=? WHERE id=?`).run((error as Error).message,now(),jobId)}}
function store(versionId:string,tier:string,content:string){db.prepare('INSERT OR REPLACE INTO context_cache(document_version_id,tier,content,token_estimate,created_at)VALUES(?,?,?,?,?)').run(versionId,tier,content,tokens(content),now())}
