import{describe,expect,it}from'vitest';import{zipSync,strToU8}from'fflate';import{readSafeZip}from'./zip.js';
describe('ZIP safeguards',()=>{it('accepts HTML with bundled safe assets',async()=>{const archive=zipSync({'index.html':strToU8('<p>Hello</p>'),'style.css':strToU8('p{color:red}')});const result=await readSafeZip(Buffer.from(archive));expect(result.htmlEntries).toEqual(['index.html']);expect(result.files.get('style.css')?.toString()).toContain('color')});it('rejects traversal paths',async()=>{const archive=zipSync({'../escape.html':strToU8('<p>bad</p>')});await expect(readSafeZip(Buffer.from(archive))).rejects.toThrow(/(?:Unsafe ZIP path|invalid relative path)/)});it('rejects extreme expansion ratios',async()=>{const archive=zipSync({'index.html':strToU8('a'.repeat(2_000_000))},{level:9});await expect(readSafeZip(Buffer.from(archive))).rejects.toThrow(/expansion limits/)});});
